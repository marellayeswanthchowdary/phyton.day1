Step-1: Start
Step-2: Take input fullname, email,num,age .
Step-:3 Find the length of fullname,email,num.
Step-4: Count “@” , “ . ” in email .
Step-5: len(fullname)<2 or check first character of fullname is space or,last character of fullname is space,  fullname     is Invalid.
Step-6: email.count(“@”) ==0 or email.count(“ ”) ==0 or Frist character of email is “@” Invalid.
Step-7: len(num)!=0 or First digit of num Is “0” check for non-numeric characters -> Num is Invalid.
Step-8: if age is between 18 and 60 age is valid otherwise age is Invalid.
Step-9: if step 5 or step-6 or step-7 or step-8 Print “User is Invalid”.Else print “User is Valid”
